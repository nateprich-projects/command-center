{"departures": [], "done": true, "summary": "Ticket #824 verified complete on this branch with no further edits needed: runners and routines read the published brief snapshot via the new offline `funnel snapshot` command instead of running a live brief (no script under scripts/ or routines/ invokes brief; only publisher.py runs it, on the LaunchAgent cadence); the fail-closed brief gate is gone so every over-budget section degrades into an explicit `degraded` record while the rest of the brief still emits and spools; claude/zcode routine breakdown sections read `.brief.awaiting_breakdown` from the snapshot; /funnel renders `.brief` plus snapshot age. Proof: full suite 1750 passed, 1 skipped (exit 0), dashboard JS 10/10, including the snapshot tests, the degrade-explicitly brief tests, and the no-live-brief-invocation guard."}