# Local development only. Production credentials belong in the external files
# documented in README.md, not in the repository or the Docker build context.
# Generate a private caller token with: openssl rand -hex 32
INBOUND_STATIC_TOKEN=replace-me
