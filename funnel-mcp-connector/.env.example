# Generate a private caller token with: openssl rand -hex 32
# Keep the generated value in this gitignored .env file; never commit it.
INBOUND_STATIC_TOKEN=replace-me

# GitHub CLI authentication for the read tools; use the existing classic token.
# Keep the real value private and never commit it.
GH_TOKEN=replace-me
