#!/usr/bin/env python3
"""review-apply — validate a review answer and perform its effects.

Usage: review-apply <pr> --repo owner/name --answer - [--ci STATE]
                     [--head SHA] [--attempt N]

Part of #794: the model answered the review question with one JSON
object, and this step validates it and performs every side effect —
record the verdict, merge an approval through the existing gate, close
the ticket. A malformed first answer exits 3 so the runner can retry
once; a malformed final answer is recorded as rejected.
"""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from engine.review_apply import main

if __name__ == "__main__":
    raise SystemExit(main())
