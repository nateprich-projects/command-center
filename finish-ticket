#!/usr/bin/env python3
"""finish-ticket — publish one structured implementation answer."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from engine.implement import finish_main

if __name__ == "__main__":
    raise SystemExit(finish_main())
