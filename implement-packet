#!/usr/bin/env python3
"""implement-packet — print one read-only implementation packet as JSON."""

import os
import sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from engine.implement import packet_main

if __name__ == "__main__":
    raise SystemExit(packet_main())
